# 兼容验证记录

验证日期：2026-09-06。环境：Windows、Node.js 22.23.2、Git、Chrome 152.0.7977.76、Playwright。Reader 0.4。两份上游代码从 GitHub 下载到临时目录，未覆盖已有 Archify。

| 上游基准 | 官方生成与 Reader 构建 | 浏览器检查 |
|---|---|---|
| 正式发布 v2.16.0 | 通过 | 在线、离线各 11 类检查通过，脚本错误 0 |
| main 固定提交 c6519401f7b91b9d43011657880893b0a8955548（包版本 2.17.0-dev.1） | 通过 | 在线、离线各 11 类检查通过，脚本错误 0 |

测试输入为同一份 `examples/campus-events/workflow.json`。六个详情节点、十三处源码片段均在浏览器中与临时 Git 快照逐字比较；源码提交为 `0e604f227dea987fbf05e6c39190174b95d92ad1`。

覆盖：原图字节保留、悬停、键盘选中、逐节点详情、关闭后原生状态保留、面板切换、原生搜索、缩放与平移、节点引用及返回、桌面浅色 1440×900、深色 2082×1315、手机 390×844 的布局。
引用返回的专门交互断言在桌面执行；三种视口都检查了布局，不能把这解释为三种视口下所有交互已穷举。

离线浏览器收据在 `checks/`。main 测试产物在 `checks/main/`，正式版产物在 `examples/campus-events/`。收据哈希指向对应增强 HTML；浏览器收据不代替人工视觉判断。人工已查看正式版桌面浅色与 main 手机截图，未见这两张图中的重叠或裁切问题。

随包测试：17 项构建测试通过；skill 基础格式检查通过。本轮未修改 Reader 运行时或 CSS，未因兼容检查放宽任何规则。

## 复跑

分别获取上述官方版本，再运行：

```text
node scripts/build-demo.mjs "<对应版本的bin/archify.mjs>"
node scripts/check-browser.cjs "<构建输出的manifest路径>"
```

浏览器脚本启用 offline 模式。构建生成的源码仓库与收据位于系统临时目录，脚本结束后保留用于检查。
示例源码只用于阅读层验证，没有运行 Python 业务服务；业务语义、并发或事务正确性未由本测试证明。

## 发布范围

目前只验证这个 Workflow 场景。未验证 Architecture、Sequence、Dataflow、Lifecycle，未验证其他浏览器及操作系统。
main 后续更新不属于本报告，即使版本号仍相同。遇到 DOM 接口变化时，需要另行复验。
本包默认示例使用正式版 v2.16.0 生成，用户也可用指定 main 提交重建。

第一次受限环境下运行官方 deliver 时，系统临时目录父路径解析被沙箱拒绝（EPERM）；在允许读取该路径的环境复跑成功。这是本机权限问题，没有据此修改上游或 Reader。
